{
  "name": "lighthouse-dashboard",
  "version": "1.0.0",
  "description": "Lighthouse CI Dashboard",
  "private": true,
  "dependencies": {
    "@lhci/cli": "0.12.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
